# Direction — FedEx Custom Critical Owner/Operator

## Active

**Resolved at:** 2026-09-24T13:00:00Z
**Status:** active

### Phrase (verbatim)

> Modern, brand-faithful visual refresh that keeps all existing copy faithful
> (restructure, not rewrite) and fixes the legacy tensions (Times New Roman
> headings vs Roboto body, ad-hoc type scale, inconsistent link colors, dated
> fragmented layout). Keep FedEx identity — purple #4D148C and orange #FF6600.
> Consolidate four source pages into ONE page: (1) intro hero, (2) tabs
> Programs / Communications / Online Tools, (3) FAQ accordion, (4) bottom
> contact block. ia-fidelity reimagined for layout; content verbatim.

### Restatement (dimensional)

- **expressive:** restrained → committed (confident modern execution)
- **distinctiveness:** familiar (stay unmistakably FedEx)
- **tone:** serious / institutional (unchanged)
- **density:** balanced (brand register, multi-audience hard floor → 64px)
- **ia-fidelity:** reimagined (layout/structure may change; **content verbatim**)
- **audience:** prospective + current owner operators, CDL drivers

### Signal classification

`signal-strong` — palette ≥ 3 distinct colors (FedEx purple, orange, greys,
link blues) + named type family (Roboto). → **Mode A (brand-faithful)** by
default; matches the user's explicit "keep FedEx identity."

### Movements

- expressive: restrained → committed
- distinctiveness: familiar (pinned — brand-faithful)
- density: `balanced` (default; multi-audience floor fired → sectionPadding ≤ 64px)
- ia-fidelity: `reimagined` (user-pinned)
- type: **pinned** to captured Roboto; Times-New-Roman heading accident
  corrected to Roboto via Mode A+ brand-adjacent refinement
- palette: **pinned** to captured FedEx purple/orange; non-brand link
  blues unified to brand palette via Mode A+ refinement

### Gaps / questions

None outstanding — the brief fully specified IA, fidelity, and brand pins.
Density and ia-fidelity did not need the one-shot follow-ups (ia-fidelity
was user-pinned; density defaulted to balanced under the multi-audience
floor).

### Divergence inputs

- mode: brand-faithful (Mode A) + Mode A+ (2 evidence-gated refinements:
  Roboto headings, unified purple/orange links)
- seed: decade 2025-now; ground-family stark-white (brand override)
- font deck: brand-inherited (Roboto)
- palette: inherited from `_brand-extraction.json`
- anti-toolbox audit: clean (Generic-2026-SaaS silhouette named as anti-ref
  in PRODUCT.md)

### Command sequence (proposed for prototype)

`craft` (initial render) → `adapt` (responsive) → critique + audit gates.
Iteration via `typeset` / `layout` / `colorize` as needed.

### Confirmation

User confirmed the plan implicitly via the detailed brief (execute mode;
minimal single-page path, visual redesign goal). Proceeding without a
further gate.

### Assumptions defaulted

- `density: balanced` (multi-audience brand register; unanswered → default)
- motion register `arrival` (civic-formal + institutional personality)
- single variant (no N-variant request)
